# No environment variables needed.
# API keys are provided by users at runtime in the browser.
# Keys are stored only in React state / sessionStorage — never sent to any backend.
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder